import React from "react";
import PropTypes from "prop-types";
import styles from './Tabs.module.scss';

const Tabs = props => {
  return (
    <div>
      I like tabs!
    </div>
  );
};

Tabs.propTypes = {

};

export default Tabs;
